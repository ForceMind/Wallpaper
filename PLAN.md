# Wallpaper 产品与实现计划

## 0. 当前状态基线

当前项目是 Swift + AppKit + SwiftPM 的 macOS 菜单栏应用，已具备：

- Bing、Unsplash、Wikimedia Commons、Picsum 和内置程序化壁纸源。
- 内置 120 套离线壁纸，在线源失败时可以回退。
- 菜单栏应用、中文/英文菜单、3×3 低清预览面板。
- 手动、固定间隔、每日定时、启动时、网络恢复、随机时间窗等更换时机。
- 流式下载、缓存清理和桌面图片设置。

已确认的问题：

1. “壁纸来源”没有明确说明它影响自动获取、预览还是手动更换。
2. 当前九宫格初始内容以内置渐变为主，用户看不到 Bing 最近图片时会误以为 Bing 没有工作。
3. “来源刷新”和“本机更换时机”在产品层没有清楚分层。
4. 每个更换时机需要可观察的触发条件、状态反馈和失败恢复规则。

## 1. 产品概念模型

应用只保留三个用户需要理解的概念：

### 1.1 壁纸来源

“来源”决定应用在**需要下一张壁纸时优先从哪里获取**，不是立即更换，也不是只过滤九宫格。

选择某个来源后：

1. 自动更换触发时，先尝试该来源。
2. 来源返回无效、超时、限流或网络不可用时，进入统一回退链。
3. 成功获取后，将图片缓存并设置为桌面壁纸。
4. 九宫格预览优先展示该来源的候选图；无法预览时展示内置候选，并标注来源。

来源刷新频率由程序自动决定：

- Bing：按 Bing 每日内容模型处理，同一天重复请求不应重复下载同一张。
- Unsplash、Wikimedia、Picsum：根据源的响应、缓存和去重结果动态处理，不暴露“源站刷新间隔”设置。
- 内置：永远可用，按日序或用户选择从至少 120 套壁纸中取图。

### 1.2 九宫格预览

九宫格是“候选选择器”，不是来源刷新器：

- 默认显示当前选择来源的候选图。
- 在线源有网时显示低清缩略图，并在卡片上标出来源名称。
- 在线源失败时显示内置低清候选，并显示“离线内置”。
- 点击卡片立即设置桌面，不改变自动更换时机，也不改变来源设置。
- 预览只解码最大约 220px 的缩略图，原图只在用户选择或自动更换时落盘。

### 1.3 更换时机

“更换时机”只决定**本机桌面什么时候换图**，不决定来源何时刷新：

- 仅手动：只在用户点击“立即更新”或九宫格卡片时更换。
- 固定间隔：按 15/30/60/180 分钟更换；只有选择此项时才显示“更换间隔”。
- 每日定时：每天在用户选择的时间更换一次；默认 09:00。
- 启动时：应用启动完成后尝试更换一次。
- 网络恢复：网络从不可用变为可用时尝试更换一次，不在网络稳定期间重复刷新。
- 随机时间窗：在用户选择的窗口内每天随机安排一次，错过目标分钟时在下一次检查立即补执行。

## 2. 来源与回退规则

统一回退链：

```text
用户首选来源
→ Bing
→ Unsplash
→ Wikimedia Commons
→ Picsum
→ 内置 120 套
```

规则：

- 同一个更新事务内，每个来源最多尝试一次。
- 成功下载后原子写入缓存，再设置桌面；任一步失败都不能覆盖当前桌面。
- 下载失败不删除上一张可用壁纸。
- 所有在线源失败时，必须确保内置源可生成并设置桌面。
- 缓存只保留有限数量的成品，预览缓存单独管理并有上限。

## 3. 状态与反馈

状态栏和控制面板必须能区分：

- 空闲：等待下一次更换。
- 获取中：正在请求来源。
- 已更换：显示来源和时间。
- 使用缓存：在线源返回了已缓存内容。
- 离线兜底：正在使用内置壁纸。
- 失败：所有来源失败，保留当前桌面并显示可重试操作。
- 暂停：自动时机不再触发，但手动预览选择仍可用。

失败提示不能只用蜂鸣声；需要在控制面板展示简短可读的错误和下一步操作。

## 4. 控制面板信息结构

从上到下：

1. 当前状态：正在获取、已更换、离线兜底或失败原因。
2. 当前来源说明：例如“首选：Bing；失败自动回退”。
3. 3×3 候选网格：低清缩略图、来源标签、点击更换。
4. “获取最新壁纸”：请求首选来源并执行统一回退链。
5. “更换时机”：展示当前时机和对应细化设置。
6. “暂停自动更新”：只暂停自动触发，不禁用手动操作。

## 5. 实现拆分

### 5.1 数据模型

- 保留 `WallpaperSource`，补充每个来源的展示名、预览能力和缓存策略元数据。
- 将现有 `UpdateStrategy` 的用户展示名统一改为“更换时机”，内部名称可以保持兼容。
- 为设置增加来源、时机、时机细化参数、语言和缓存上限的版本化解码。
- 为 `WallpaperImage` 增加可选的显示来源标签和缓存状态。

### 5.2 来源层

- `WallpaperProvider` 统一提供单张获取和候选预览获取。
- Bing 支持当天单张更新和最近多张预览，两者不能混用缓存键。
- 在线预览使用低清 URL 或落盘后 ImageIO 缩略图解码。
- 内置源提供可重复、可编号的至少 120 套壁纸，并支持离线直接生成。

### 5.3 调度层

- 为每种更换时机定义独立触发器和去重键。
- 每次切换时机先取消旧定时器/监听器，再创建新触发器。
- 网络恢复使用 `NWPathMonitor`，记录可用/不可用边沿，避免重复触发。
- 每日和随机窗口保存“当天是否已执行”状态，跨重启时按当前时间补偿一次。

### 5.4 UI 层

- 九宫格使用固定卡片尺寸，缩略图铺满卡片，不依赖 `NSButton` 的内在高度。
- 卡片显示来源标签和加载占位状态。
- “更换间隔”只在固定间隔模式插入菜单。
- 每日时间和随机窗口只在对应时机插入细化菜单。
- 语言切换后重建菜单和控制面板文案。

## 6. 内存、磁盘和网络约束

- 原图下载必须使用 `URLSession.download`，禁止把原图整体读入 `Data`。
- 预览最大解码尺寸约 220px，界面同时保留最多 9 张缩略图。
- 自动更换事务最多保留一张下载临时文件和轻量元数据。
- 成品缓存、预览缓存、内置生成缓存分目录管理，并分别设置上限。
- 在线请求设置超时、失败回退和重复请求去重。

## 7. 验收标准

功能：

- 选择 Bing 后，自动更换优先使用 Bing；失败时能按回退链继续工作。
- 九宫格有网时能看到 Bing 或其他在线候选，无网时能看到内置候选。
- 点击九宫格任意卡片后，当前桌面发生变化，且来源标签正确。
- 固定间隔之外不显示“更换间隔”。
- 每日、网络恢复、随机窗口均可配置、可触发、可去重、可恢复。
- 手动选择不会意外修改自动更换时机。

体验：

- 所有按钮和状态文案中文默认可读，English 切换完整。
- 加载、失败、离线、暂停状态都有明确反馈。
- 低清缩略图不出现被压成横条、空白或不可辨识的问题。

工程：

- SwiftPM release 构建通过。
- App Bundle 图标和资源完整。
- 关键逻辑有可重复的单元/集成验证或明确记录未覆盖范围。
- README、Release 说明与实际功能一致。

## 8. 本轮实施顺序（待确认后执行）

1. 先修正来源选择的产品语义、状态模型和文案。
2. 重构候选预览，使其在线优先、内置兜底并显示来源标签。
3. 完善每日、网络恢复、随机窗口的触发器和细化菜单。
4. 补齐控制面板加载/失败/离线状态。
5. 添加必要的逻辑验证，构建并做实际界面检查。
6. 更新 README、版本号、Release 和本机安装。

## 9. 执行记录

- 已确认产品语义：来源控制“优先从哪里取”，更换时机控制“什么时候换”。
- 已完成来源目录与预览支持模块：在线候选优先、低清缩略图、内置 120 套兜底。
- 已完成独立更换时机调度器：每日、固定间隔、启动、网络恢复、随机窗口和暂停语义。
- 已完成控制面板、状态反馈和高质量手动应用路径整合：预览只显示低清缩略图，点击候选会下载原图再设置桌面。
- 已完成来源选择语义：首选来源影响自动获取优先级和预览候选，不改变更换时机；来源失败统一回退。
- 已完成更换时机整合：手动、固定间隔、每日、启动、网络恢复、随机窗口，并按策略显示对应细化设置。
- 待验收：真实界面逐项点击、模拟在线源失败的离线回退、内存/缓存测量、发布包和本机安装验证。
